mixin ratingForm(story)
  form.ratings(action=`/rating/${story._id}?rating=true` method="POST")
    input(type='hidden' name='story' value=story._id)
    button(type='submit' class='button__rating') Hey, that's ok
    button(type='submit' class='button__rating' formaction=`/rating/${story._id}?rating=false`) No way, not ok
